# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    app.medications_file_path: '%kernel.project_dir%/resources/medications.json'
    # Base du lien envoyé dans l'email de réinitialisation (ML-78) : l'origine
    # du frontend web, jamais celle de l'API.
    app.frontend_url: '%env(FRONTEND_URL)%'
    app.mailer_from_address: '%env(MAILER_FROM_ADDRESS)%'
    # Schéma de deep link de l'app mobile (ML-78) : utilisé à la place de
    # app.frontend_url quand la demande de réinitialisation vient du mobile.
    app.mobile_app_scheme: '%env(MOBILE_APP_SCHEME)%'
    # Version courante et URL de l'APK (ML-98), exposées via GET /api/app-version
    # pour l'update-checker mobile.
    app.version: '%env(APP_VERSION)%'
    app.apk_download_url: '%env(APK_DOWNLOAD_URL)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
    App\Service\MedicationReferenceService:
        arguments:
            $medicationsFilePath: '%app.medications_file_path%'

    App\Service\PasswordResetMailer:
        arguments:
            $frontendUrl: '%app.frontend_url%'
            $mobileScheme: '%app.mobile_app_scheme%'
            $fromAddress: '%app.mailer_from_address%'

    App\Controller\AppVersionController:
        arguments:
            $version: '%app.version%'
            $apkDownloadUrl: '%app.apk_download_url%'
